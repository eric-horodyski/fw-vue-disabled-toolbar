import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw-vue-disabled-toolbar',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
